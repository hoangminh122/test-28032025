export enum NODE_ENV {
  DEVELOP = 'development',
  LOCAL = 'local',
  PRODUCTION = 'production',
  STAGING = 'staging',
  UAT = 'uat',
}

export enum TIME_FORMAT_TYPE {
  YYYY_MM_DD_HH_MM_SS = 'YYYY-MM-DD HH:mm:ss',
}

export enum SORT_DIRECTIONS {
  ASC = 'ASC',
  DESC = 'DESC',
}
